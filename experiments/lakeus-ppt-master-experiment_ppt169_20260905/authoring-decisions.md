# Experiment decisions

The user delegated end-to-end execution and requested a separate ppt-master experiment. Existing Finnish content, audience, timing and browser gallups supply the brief. No new confirmation receipt is claimed.

Three considered directions: (A) technical workshop: navy canvas, lime steps, Arial and Courier New, tool-led diagrams; (B) editorial field guide: warm paper, navy evidence figures, blue actions, asymmetric Arial typography, existing illustrations, varied research and practical compositions; (C) magazine essay: warm paper, Georgia headlines and Arial body, wide illustration spreads, fewer persistent diagrams. B is selected to compare a clearly different design with the same content. Existing illustrations are reused, with no new image acquisition.

Font calibration, approximate Latin characters per 100px: body32 6.3; title48 4.2; subtitle40 5.0; annotation24 8.4; code28 5.5; display96 2.1; footnote20 10.1. Keep additional fitting headroom and inspect rendered results.

Live preview startup attempted before P01: server reported no free TCP port 6060–6109. Continue with static rendering and native PowerPoint review.

Page decisions before authoring (job → topology → composition → geometry signature):

- P01 modules: core, image. Hook → no → native title on quiet illustration → left title / full field. A shaped opening would hide useful setting; no drawn carrier needed.
- P02 modules: core, structure. Delegate verifiable work → yes contrast → uneven two-column request → small explanation / large action. Opposing flat fields preserve literal peer contrast; chevrons would imply progression.
- P03 modules: core, structure. Show implementation membership → yes membership → one work field with four labeled regions → blue implementation region in wider navy field. Separate rectangles preserve independent work areas; nested ellipse would imply containment between stages.
- P04 modules: core, structure. Explain bounded experiment → yes order → numbered chain → three stops on one line. A block arrow would overstate causal guarantees; ordinal stops convey the procedure.
- P05 modules: core, structure. Compare study boundaries → yes contrast → two large independent numbers → shared baseline separated by rule. Avoid one axis because outcomes differ.
- P06 modules: core, structure. Locate waiting → yes order → implementation to use with review gap → open sequence and waiting callout. Relative lengths are not measured time.
- P07 modules: core, visualization, chart, native-data. Distinguish learning outcome → yes contrast → two bars on common scale → literal data lengths 50 and 67 of 100.
- P08 modules: core, image, structure. Show possible use → yes link → square illustration plus editable record → picture and record joined by proximity. Document rectangle is literal record boundary; folded-corner preset adds no semantic information.
- P09 modules: core, structure. Make learning intentional → yes order → staggered four-step path → one reading direction, no claimed closed cycle.
- P10 modules: core, structure. Separate sector observation and interpretation → yes contrast → hero statistic plus local capabilities → dominant evidence and narrower interpretation.
- P11 modules: core, structure. Start practical act → yes order → dark five-step route → large task above numbered stages.
- P12 modules: core, structure. Teach task brief → yes membership → four ruled lines → labels align to example clauses. No grid cell facts, no table.
- P13 modules: core, structure. Ground agent in repo → yes link → repository and instruction files → paired code/text regions. Literal file tree uses indentation, no decorative hierarchy edges.
- P14 modules: core, structure. Explain tools → yes link → agent left, four capabilities right → shared junction and four straight edges. No curves required by routing.
- P15 modules: core, structure. Reproduce lost field → yes contrast/order → two same-form states → constant positions and explicit absent value.
- P16 modules: core, structure. Connect change and checks → yes link → code with emphasized line beside checks → literal code panel and three independent conditions.
- P17 modules: core, structure. Teach review handoff → yes membership → cause/change/evidence with open decision → ruled note and separate unresolved item.
- P18 modules: core, structure. Learn from user correction → yes order → note, draft, corrected decision → increasing textual emphasis, not measured sizes.
- P19 modules: core, structure. Bound parallelism → yes link/order → two independent tasks meet integration → two branches and common destination.
- P20 modules: core, structure. Continue after release → yes order → observation to check with owner → ordered steps and accountable recipient.
- P21 modules: core. Practice understanding → no → one large question with small procedure → typography only, no container needed.
- P22 modules: core, structure. Choose next task → yes order → three concise actions and browser handoff → numbered horizontal sequence.
- P23 modules: core. Close on responsibility → no → oversized thesis → dark field and native type; no contour carries additional meaning.

Carrier receipt: rectangles express literal fields, code and forms; plain lines express sequence and links; bars express numeric values. Inflected presets, Boolean silhouettes and filters would add semantics or decoration without serving these pages. Inline emphasis is used for the core contrast and changed code line. Existing textured images supply material variety without new effects.
